{
  "schemaVersion": 1,
  "stack": "event-rsvp",
  "workload": "web",
  "kind": "app",
  "runtime": "node",
  "title": "Event + RSVP",
  "description": "A shareable event page with a public RSVP form and a live guest list. Postgres-backed.",
  "port": 3000,
  "buildType": "dockerfile",
  "database": { "engine": "postgres", "provider": "deploymill" }
}
